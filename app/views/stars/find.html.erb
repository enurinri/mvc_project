<h1 class="display-4 text-primary">Stars#Find</h1>
<p><%= @msg %></p>
<%= form_tag(controller: "stars", action: "find") do %>
<div class="form-group">
    <label for="find">Find</label>
    <%= text_field_tag("find", '' , {class:"form-control"}) %>
</div>
<%= submit_tag("Click")%>
<% end %>

<table class="table">
    <tr>
        <th>Id</th><th>Name</th><th>Age</th><th>Mail</th>
    </tr>
    <% @stars.each do |obj| %>
    <tr>
        <td><%= obj.id %></td>
        <td><%= obj.name %></td>
        <td><%= obj.age %></td>
        <td><%= obj.mail %></td>
    </tr>
    <% end %>
</table>
